# Dropdown Menu

Dropdown menus are useful for displaying a list of actions or links in a popover.

The popover is rendered outside of the component (using a [Portal](https://reactjs.org/docs/portals.html)), but focus is handled correctly by the component.
